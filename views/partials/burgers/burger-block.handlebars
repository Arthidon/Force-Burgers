<li>
	{{name}}

	<button class="btn btn-outline-secondary change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}<i class="far fa-check-square"></i>{{else}}Eat Again!{{/if}}
	</button>

	<button class="btn btn-outline-secondary delete-burger" data-id="{{this.id}}"><i class="far fa-trash-alt"></i></button>
</li>
<!-- <button class="btn btn-outline-secondary" style="width: 45px; color: white;x"> -->